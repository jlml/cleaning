<%= form_for @order, url: wizard_path do |f| %>
	<h1>Cost: </h1><%= @order.price%>
	  <div class="field">
    <%= f.label :cleaner %><br />
    <%= f.text_field :cleaner%>
  </div>
  <div class="actions">
          <%= f.submit "Continue" %>
  </div>
<% end %>